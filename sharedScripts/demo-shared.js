// @ts-check
/**
 * You can also import another NPM package
 * const _ = require("lodash");
 * const moment = require("moment");
 *
 * you may replace with ES6 Class, function(s) or anything
 *
 */

const demoShared = {
  demo: () => "return a string",
  run: () => 2020,
  book: "book is string"
};

module.exports = demoShared;
